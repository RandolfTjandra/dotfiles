interface:
  display_name: "Stash Belongings"
  short_description: "Route belongings and storage tasks to stash"
  default_prompt: "Use $stash-belongings to track and locate household belongings in stash."
dependencies:
  tools:
    - type: "mcp"
      value: "stash"
      description: "Home inventory and storage MCP"
policy:
  allow_implicit_invocation: true
