interface:
  display_name: "Issue Next"
  short_description: "Summarize tracker state and recommend next work"
  default_prompt: "Use $issue-next to summarize the current repo's planning tracker and recommend the next issue to work on."
policy:
  allow_implicit_invocation: false
