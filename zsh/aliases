# Aliases
alias godots="cd ${BASH_STUFF}"
alias l="gls -aFlh --group-directories-first --color"
alias matrix="cmatrix -absr"
alias pipes="pipes.sh -p 3 -t 0 -R"
alias vim="nvim"

# Work related
alias projects='cd /Users/rtjandra/Dev/projects/cx-projects'
alias gogo='cd /Users/rtjandra/Dev/projects/cx-projects/golang/src/github.com'
alias gocover="go test ./... -coverprofile cover.out && go tool cover -html=cover.out -o coverage.html && go tool cover -func cover.out | grep total"
alias gocover="go test ./... -coverprofile cover.out && go tool cover -html=cover.out -o coverage.html && go tool cover -func cover.out | grep total"

alias office="yabai -m rule --add app="^Cron$" space=6;yabai -m rule --add app="^Slack$" space=6;yabai -m rule --add app="^Kitty$" space=2"

## Projects
alias pay='cd /Users/rtjandra/Dev/projects/cx-projects/dockertest/secure-payments'
alias efc='cd /Users/rtjandra/Dev/projects/cx-projects/foobar/ellation-formation-c'
alias ef='cd /Users/rtjandra/Dev/projects/cx-projects/ellation_formation'

# Opens jira page for current branch
function jira() {
    python -mwebbrowser https://jira.tenkasu.net/browse/$(git branch | grep '*' | sed -E 's/\* draft-//;s/([a-zA-Z]+-[0-9]+).*/\1/')
}

