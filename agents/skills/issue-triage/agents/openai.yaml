interface:
  display_name: "Issue Triage"
  short_description: "Move issues between backlog, active, and done"
  default_prompt: "Use $issue-triage to update issue status in the current repo's planning tracker."
policy:
  allow_implicit_invocation: false
